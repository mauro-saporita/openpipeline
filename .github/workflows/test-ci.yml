name: test ci

on:
  pull_request:
  push:

jobs:
  list_components:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0 

    - uses: viash-io/viash-actions/setup@main
    - name: Fetch viash
      run: |
        viash -h